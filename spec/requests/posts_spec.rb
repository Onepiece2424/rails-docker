# require 'rails_helper'

# RSpec.describe "投稿ページ", type: :request do
#   let(:post) { create(:post) }
#   let(:user) { create(:user) }
#   let(:post_user) { create(:post, user: user)}
#   before do
#     get post_url post.id
#   end

#   it 'レスポンスが返ってくること' do
#     expect(response.status).to eq(200)
#   end

#   it 'レスポンスに投稿者名が含まれること' do
#     expect(response.body). to include post_user.name
#   end
# end
